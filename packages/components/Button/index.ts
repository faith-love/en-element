import Button from './Button.vue';
import ButtonGroup from './ButtonGroup.vue';
import { withInstall } from '@en-ui/utils';
export const EnButton = withInstall(Button);
export const EnButtonGroup = withInstall(ButtonGroup);
export * from './types';
